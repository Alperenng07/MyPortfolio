@using Entities
@model Entities.Education

<div class="myCard">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">
                    <i class="fas fa-text-width"></i>
                    Egitim Detay
                </h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-4">Id: </dt>
                    <dd class="col-sm-8">@Model.Id</dd>
                    <dt class="col-sm-4">Okul Adı: </dt>
                    <dd class="col-sm-8">@Model.Name</dd>
                    <dt class="col-sm-4">Bolum Adı: </dt>
                    <dd class="col-sm-8">@Model.Departmant</dd>

                    <dt class="col-sm-4">Sehir: </dt>
                    <dd class="col-sm-8">@Model.City</dd>
                    <dt class="col-sm-4">Ulke </dt>
                    <dd class="col-sm-8">@Model.Country</dd>
                    <dt class="col-sm-4">Mezuniyet Tarihi </dt>
                    <dd class="col-sm-8">@Model.GraduateDate</dd>


                </dl>
            </div>
            <!-- /.card-body -->
            <div class="card-footer">
                <a type="button" href="@Url.Action("Index", "Education")" class="btn btn-primary">Geri</a>
            </div>
        </div>
        <!-- /.card -->
    </div>
</div>
<style>
    .myCard {
        width: 1200px;
        height: 200px;
        padding-left: 250px;
    }
</style>
