@using Entities
@model Entities.Skill

<div class="myCard">
    <div class="col-md-12">
        <!-- general form elements -->
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title">Yetenek Düzenle</h3>
            </div>
            <!-- /.card-header -->
            <!-- form start -->
            <form asp-controller="Skill" asp-action="Update" method="post">
                <div class="card-body">


                
                    <div class="form-group">
                        <label for="name">Yetenek Adı:</label>
                        <input name="name" value="@Model.name" type="text" class="form-control" id="name" placeholder="Yetenek Adı">
                    </div>
                   
                    <div class="form-group">
                        <label for="Level">Seviye:</label>
                        <input name="Level" value="@Model.Level" type="text" class="form-control" id="Level" placeholder="Seviye">
                    </div>
                    <div class="form-group">
                        <label for="Score">Skor:</label>
                        <input name="Score" value="@Model.Score" type="text" class="form-control" id="Score" placeholder="Skor">
                    </div>
                   

                </div>
                <!-- /.card-body -->
                <div class="card-footer">
                    <a type="button" href="@Url.Action("Index", "Skill")" class="btn btn-primary">Geri</a>
                    <button type="submit" class="btn btn-success">Düzenle</button>
                </div>
            </form>
        </div>
        <!-- /.card -->
    </div>
</div>
<style>
    .myCard {
        width: 1200px;
        height: 200px;
        padding-left: 250px;
    }
</style>
